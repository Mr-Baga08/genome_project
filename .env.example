# ============================================================================
# QUICK START CONFIGURATIONS
# ============================================================================
# Choose one of these configurations based on your setup needs
# Copy the relevant section to your .env file

# ============================================================================
# 🚀 OPTION 1: DOCKER DEVELOPMENT (Recommended for quick start)
# ============================================================================
# Use this configuration when running with docker-compose up -d

ENVIRONMENT=development
DEBUG=true

# Database (Docker containers)
MONGO_USER=admin
MONGO_PASSWORD=password123
MONGO_HOST=mongodb
MONGO_PORT=27017
MONGO_DB=ugene_workflows

# Redis (Docker container)  
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_URL=redis://redis:6379/0

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
REACT_APP_API_URL=http://localhost:8000

# CORS for development
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# File Storage (Docker volumes)
UPLOAD_DIR=/app/uploads
UGENE_WORKDIR=/tmp/ugene_workdir

# UGENE Settings
UGENE_IMAGE=ugene_sdk_docker_image
MAX_PARALLEL_TASKS=3
TASK_TIMEOUT=1800

# Worker Settings
MAX_WORKERS=2
WORKER_TIMEOUT=3600

# Security (development only)
SECRET_KEY=6fb54b157b669209e81ad927e427506077316f3dde2e224868691bfb1fcd2fea
SECURE_COOKIES=false

# ============================================================================
# 🖥️ OPTION 2: LOCAL DEVELOPMENT (No Docker)
# ============================================================================
# Use this when running services directly on your machine

ENVIRONMENT=development
DEBUG=true

# Local Database
MONGO_USER=admin
MONGO_PASSWORD=password123
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_DB=ugene_workflows

# Local Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_URL=redis://localhost:6379/0

# API Configuration
API_HOST=localhost
API_PORT=8000
REACT_APP_API_URL=http://localhost:8000

# Local File Storage
UPLOAD_DIR=./uploads
UGENE_WORKDIR=./tmp/ugene_workdir
STATIC_DIR=./static
CACHE_DIR=./cache

# UGENE Settings (adjust path to your UGENE installation)
UGENE_IMAGE=ugene_sdk_docker_image
UGENE_TIMEOUT=3600

# Worker Settings
MAX_WORKERS=2
QUEUE_NAME=default

# ============================================================================
# 🏭 OPTION 3: PRODUCTION DEPLOYMENT
# ============================================================================
# Use this configuration for production environments

ENVIRONMENT=production
DEBUG=false

# Production Database (use strong passwords!)
MONGO_USER=ugene_admin
MONGO_PASSWORD=YOUR_STRONG_MONGO_PASSWORD_HERE
MONGO_HOST=your-mongodb-host.com
MONGO_PORT=27017
MONGO_DB=ugene_workflows_prod

# Production Redis
REDIS_HOST=your-redis-host.com
REDIS_PORT=6379
REDIS_PASSWORD=YOUR_STRONG_REDIS_PASSWORD_HERE
REDIS_URL=redis://:YOUR_STRONG_REDIS_PASSWORD_HERE@your-redis-host.com:6379/0

# Production API
API_HOST=0.0.0.0
API_PORT=8000
REACT_APP_API_URL=https://your-domain.com/api

# Production CORS
ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# Security Settings
SECRET_KEY=YOUR_SUPER_SECRET_JWT_KEY_MINIMUM_32_CHARACTERS
SECURE_COOKIES=true
SSL_ENABLED=true
RATE_LIMIT_ENABLED=true

# Production File Storage
UPLOAD_DIR=/var/lib/ugene/uploads
UGENE_WORKDIR=/var/lib/ugene/workdir
LOG_FILE=/var/log/ugene/ugene_workflow.log

# Performance Settings
MAX_WORKERS=8
MAX_PARALLEL_TASKS=10
CACHE_ENABLED=true
ENABLE_GZIP=true

# Monitoring
METRICS_ENABLED=true
HEALTH_CHECK_INTERVAL=30
BACKUP_ENABLED=true

# ============================================================================
# 🧪 OPTION 4: TESTING ENVIRONMENT
# ============================================================================
# Use this for running tests and CI/CD pipelines

ENVIRONMENT=testing
DEBUG=true

# Test Database
MONGO_USER=test_user
MONGO_PASSWORD=test_password
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_DB=ugene_workflows_test

# Test Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_URL=redis://localhost:6379/1

# Test API
API_HOST=localhost
API_PORT=8001
REACT_APP_API_URL=http://localhost:8001

# Fast test settings
TASK_TIMEOUT=60
MAX_WORKERS=1
WORKER_TIMEOUT=120

# Test data
USE_TEST_DATA=true
AUTO_CLEANUP_DAYS=1

# ============================================================================
# 🚢 KUBERNETES/CLOUD DEPLOYMENT
# ============================================================================
# Use this when deploying to Kubernetes or cloud platforms

ENVIRONMENT=production
DEBUG=false

# Use environment-specific service names
MONGO_HOST=ugene-mongodb-service
REDIS_HOST=ugene-redis-service

# Cloud-specific settings
MONGODB_URL=${MONGODB_CONNECTION_STRING}
REDIS_URL=${REDIS_CONNECTION_STRING}

# Load balancer settings
API_HOST=0.0.0.0
API_PORT=8000
ALLOWED_ORIGINS=${FRONTEND_URLS}

# Resource limits for containers
BACKEND_MEMORY_LIMIT=2Gi
WORKER_MEMORY_LIMIT=4Gi
MAX_WORKERS=4

# Cloud storage
CLOUD_STORAGE_ENABLED=true
AWS_BUCKET_NAME=${S3_BUCKET_NAME}
UPLOAD_DIR=/tmp/uploads

# Monitoring
PROMETHEUS_PORT=9090
METRICS_ENABLED=true
LOG_LEVEL=INFO

# ============================================================================
# 💻 MINIMAL DEVELOPMENT SETUP (For quick testing)
# ============================================================================
# Absolute minimum configuration for development

ENVIRONMENT=development
MONGO_PASSWORD=password123
REDIS_URL=redis://localhost:6379
REACT_APP_API_URL=http://localhost:8000
SECRET_KEY=dev-key-123
UPLOAD_DIR=./uploads

# ============================================================================
# 🔒 SECURITY-FOCUSED CONFIGURATION
# ============================================================================
# Enhanced security settings for sensitive environments

ENVIRONMENT=production
DEBUG=false

# Strong authentication
SECRET_KEY=your-256-bit-secret-key-here-minimum-32-chars
ACCESS_TOKEN_EXPIRE_MINUTES=15
SESSION_EXPIRE_HOURS=8

# Enhanced security
SECURE_COOKIES=true
RATE_LIMIT_PER_MINUTE=30
VIRUS_SCAN_ENABLED=true
SSL_ENABLED=true

# File restrictions
MAX_UPLOAD_SIZE=52428800  # 50MB
ALLOWED_EXTENSIONS=.fastq,.fasta,.csv
AUTO_CLEANUP_DAYS=3

# Monitoring and alerting
LOG_LEVEL=WARNING
METRICS_ENABLED=true
SLACK_ENABLED=true
SLACK_WEBHOOK_URL=your-slack-webhook-url

# ============================================================================
# 🏃‍♂️ PERFORMANCE-OPTIMIZED CONFIGURATION  
# ============================================================================
# High-performance settings for large-scale deployments

ENVIRONMENT=production

# Database optimization
DB_POOL_SIZE=50
DB_MAX_OVERFLOW=75
MONGO_MAX_CONNECTIONS=200

# Redis optimization
REDIS_MAX_CONNECTIONS=50

# Worker optimization
MAX_WORKERS=16
MAX_PARALLEL_TASKS=20
WORKER_TIMEOUT=7200

# Cache optimization
CACHE_ENABLED=true
CACHE_TTL=7200
CACHE_MAX_SIZE=10000

# Resource limits
BACKEND_MEMORY_LIMIT=8g
WORKER_MEMORY_LIMIT=16g
CPU_LIMIT=4.0

# Performance monitoring
PERFORMANCE_MONITORING=true
SLOW_QUERY_THRESHOLD=500